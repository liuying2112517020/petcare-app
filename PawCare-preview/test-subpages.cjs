const {chromium}=require('C:/Users/Lenovo/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:393,height:852}});await p.goto('http://127.0.0.1:4173');await p.locator('[data-open="care"]').first().click();assert.equal(await p.locator('#sheet').evaluate(e=>e.matches(':modal')),false);const box=await p.locator('#sheet').boundingBox();assert.ok(box.height>700);await p.locator('#close').click();assert.equal(await p.locator('#scroll').evaluate(e=>e.inert),false);await p.locator('nav [data-nav="pet"]').click();await p.locator('.owner-card').click();const title=await p.locator('#sheet-title').innerText();await p.locator('[data-account="edit"]').click();await p.locator('#close').click();assert.equal(await p.locator('#sheet-title').innerText(),title);await p.locator('#close').click();assert.equal(await p.locator('#sheet').isVisible(),false);console.log('PASS full-page size, no modal backdrop, nested back navigation, main page restored');await b.close();})();
