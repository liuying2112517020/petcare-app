const {chromium}=require('C:/Users/Lenovo/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:4173');await p.locator('nav [data-nav="medicine"]').click();await p.locator('[data-med-settings]').first().click();await p.locator('[name="count"]').selectOption('3');for(const [i,t] of ['09:00','14:00','21:00'].entries())await p.locator('[name="time'+i+'"]').fill(t);await p.locator('[name="remind0"]').uncheck();await p.locator('#edit-med-schedule [type="submit"]').click();const card=p.locator('.med-card').first();assert.equal(await card.locator('.dose-row').count(),3);assert.match(await card.innerText(),/每日 3 次/);assert.match(await card.locator('.past-dose-records').textContent(),/08:00/);await p.reload();await p.locator('nav [data-nav="medicine"]').click();assert.match(await p.locator('.med-card').first().innerText(),/21:00/);console.log('PASS schedule, frequency, history preserved, persisted');await b.close();})();

