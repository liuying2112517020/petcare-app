const {chromium}=require('C:/Users/Lenovo/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:393,height:852}});let errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4173');await p.locator('[data-open="record"]').first().click();await p.locator('[data-visit-voice]').click();await p.locator('[data-visit-transcribe]').click();await p.locator('#voice-visit-form [type="submit"]').click();assert.equal(await p.locator('h1').innerText(),'就诊记录详情');assert.match(await p.locator('.prescription-summary').innerText(),/每日 2 次/);await p.locator('[data-action="make-plan"]').click();await p.locator('[name="time0"]').fill('09:30');await p.locator('[name="time1"]').fill('21:30');await p.locator('[name="remind"]').uncheck();await p.locator('#visit-plan-form [type="submit"]').click();const med=p.locator('.med-card').last();assert.match(await med.innerText(),/09:30/);assert.match(await med.innerText(),/21:30/);assert.equal(await med.locator('[data-remind]').first().innerText(),'提醒我');assert.deepEqual(errors,[]);console.log('PASS confirmation returns to visit, prescription, editable schedule, reminders off, plan creation');await b.close();})();
